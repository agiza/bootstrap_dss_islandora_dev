
# The Travis CI configuration file

language: ruby
rvm:
  - 2.0.0p247
before_install: bash.d/drush_test_run.sh # This script executes the Drush Unit tests before executing the functional tests
script: bundle exec cucumber # Run the Selenium tests using Cucumber
